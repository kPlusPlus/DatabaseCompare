
 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END

 Nerazduzeno
					K)
					WHERE  dbo.Skladiste.ZalihaID IS NULL
					GROUP BY EvidID) 
			NDE ON dbo.Evid.ID = NDE.EvidID
	WHERE dbo.List.ID = @inListID And dbo.Evid.ListID = @inListID
	ORDER BY dbo.Evid.ID
END
