
e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END

e when @snDatum is null then dbo.Dokum.DatumPI else @snDatum end

END
