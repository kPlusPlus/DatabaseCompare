
 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END

 nvarchar) + ')'
	  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
  END

  CLOSE cPS
  IitError
  DEALLOCATE cPS
  IitError

	SELECT @nvTekst = 'zatvoren cursor kandidata za temeljnicu poŔetnog stanja'
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
ExitError:

	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	RETURN -1

ExitSucces:

	SELECT @nvTekst = 'ZAVRŐETAK BEZ GREŐKE'
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = GETDATE(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	RETURN 1
END
