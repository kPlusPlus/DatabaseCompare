
tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

tavke ist on ds.ID = ist.DokumID
				inner join dbo.DokStavka dst on ds.ID = dst.DokumID
				where l.ListNadID  = @inListNadID
			)
		end
	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	where ID in (SELECT dbo.DokStart.ID
		 		FROM dbo.List
		 		INNER JOIN dbo.DokStart ON dbo.List.ID = dbo.DokStart.ListID
				WHERE  (dbo.List.ListNadID = @inListNadID)) 

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END
