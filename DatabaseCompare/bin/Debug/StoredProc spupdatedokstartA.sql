
tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart

tatus.DokVrsta
						WHERE DokVrsta.Sifra = @tiDokVrsta And DokVrsta.Status = 0),0)
					ELSE @siStatus
					END

	WHERE [ID] = @inIDOld
		

TION t_spUpdateDokStart
