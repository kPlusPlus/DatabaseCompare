
e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError

e when @inError >  else @inError end
where ID = @inObradaID

lEit:
if cursor_status('local', 'cuD') <> -1
begin
	close cuD
	deallocate cuD
end
if @inError > 0
	raiserror (
N'
	Data processing can''t continue. 
	1 data processing log.
	
	Origin: spProcPayroll_Spec2
',16,1)

return @inError
