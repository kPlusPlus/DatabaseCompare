
 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 

 Kontakt,
k.Tel, k., k.Mobile, 
email = coalesce(email1, email2, email3)

FROM  dbo.Analitika a
INNER JOIN dbo.PartnerStart ps on a.VezaID = ps.ID
LE 
LE 
LEFT OUTER JOIN dbo.Kontakt k on kv.KontaktID = k.ID
WHERE a.ID = @inAnalitikaID AND a.AnalitikaVrsta = 2
ORDER BY k.ID 

END 
