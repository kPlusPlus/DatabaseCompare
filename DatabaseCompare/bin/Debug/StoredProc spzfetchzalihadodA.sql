
SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID

SET NOCOUNT ON

SELECT [ZalihaID], [BrutoTezina], [NetoTezina], [TempSklad], [Bach], [Trajanje], 
	[AnalitProizID], [Napomena]
FROM [dbo].[ZalihaDod] 
WHERE [ZalihaID] = @inZalihaID
