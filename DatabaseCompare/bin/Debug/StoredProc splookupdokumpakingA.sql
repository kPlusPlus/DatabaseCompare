
 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			

 nvarchar(4))
								+ '-' + RTRIM(ds.Broj) + ')' else @nvName end
				) 
		

			
