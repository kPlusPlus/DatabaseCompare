
tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END

tavke'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
				

  TION

	UPDATE dbo.DokStart
	SET Azuriranje = 1,
			Kontrola = 1
	FROM dbo.DokStart
	INNER JOIN dbo.List ON dbo.DokStart.ListID = dbo.List.ID
	WHERE dbo.List.ListNadID = @inListNadID

	SELECT @nvTekst = '	zavrÜno a×uriranje u tabeli DokStart'  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)

	GOTO ExitSucces
lRollback:
  ROLLBATION
  RAISERROR 50004 'Undefined error while preparing accounting payrolls! '
	GOTO ExitError

ExitError:

  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = 
	WHERE ID = @inObradaID

	GOTO TheEnd
ExitSucces:

	SELECT 	@nvTekst = 'ZAVRŐETAK BEZ GREŐKE', @inReturnStatus = 0
  INSERT INTO dbo.ObradaLog (ObradaID, Tekst) VALUES (@inObradaID, @nvTekst)
	PRINT @nvTekst

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID
TheEnd:
  RETURN @inReturnStatus
	RETURN 0

END
