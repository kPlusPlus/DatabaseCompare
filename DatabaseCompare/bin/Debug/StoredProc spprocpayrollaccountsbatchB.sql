
 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END

 tbDokStart
	WHERE dbo.DokStart.ID = tbDokStart.ID

  RETURN 1

ExitStatus:

  UPDATE dbo.Obrada
	SET Stop = getdate(), 
			ErrorLevel = @tiErrorLevel
	WHERE ID = @inObradaID

END
