
ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 

ne sa izradom dokumenata pa ne mogu suradnicima staviti odgovarajuci cjenik jer je 
istekao prema getdate-u.
**ako se ne izabere PArGrupa onda vrijedi za svih s tim da se pusi na grupi (none)=0 !!!! */
IF @siParGrupa = 0 
BEGIN
	UPDATE dbo.Partner
	SET jenik
	FROM dbo.Partner
	INNER JOIN dbo.PartnerStart with(nolock) on dbo.Partner.PartnerID = dbo.PartnerStart.ID
	WHERE 	formalni= @btFormalni 
		and osoba = @btOsoba 
		and radnik = @btRadnik 
		and suradnik = @btSuradnik

END
COMMIT TRAN
SET XA

END 
