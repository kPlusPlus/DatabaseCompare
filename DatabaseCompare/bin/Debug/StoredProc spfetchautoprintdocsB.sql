
select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 

select 
	dsl.ID DokumID
from dbo.DokStart ds with (nolock) 
	inner join dbo.DokStart dsl  with (nolock) on ds.ListID = dsl.ListID
	inner join dbo.DokVrsta dvl  with (nolock) on dsl.DokVrsta = dvl.Sifra
where ds.ID = @inDokumID and dvl.AutoPrnt = 1
	and dsl.Prt < 1 
